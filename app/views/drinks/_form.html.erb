<%= form_with(model: drink, local: true) do |form| %>
  <% if drink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drink.errors.count, "error") %> prohibited this drink from being saved:</h2>

      <ul>
        <% drink.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :business %>
    <%= form.text_field :business %>
  </div>

  <div class="field">
    <%= form.label :website %>
    <%= form.text_area :website %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
